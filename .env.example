# Public production origin used for canonical metadata, Open Graph URLs,
# robots.txt, and sitemap.xml. Leave empty for local development if needed.
NEXT_PUBLIC_SITE_URL=

# Optional: enables privacy-conscious Vercel Web Analytics in production.
# Enable only after Web Analytics is configured for the deployment project.
NEXT_PUBLIC_ENABLE_ANALYTICS=false
